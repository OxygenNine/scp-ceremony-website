# 2026-09-11

## 2026 节目单全量录入 + 公告栏页

用户人工过了一遍正片，给出 41 个单品的 BVID，另有主持人与一批特殊情况。

### 交付

- **`src/data/program.ts`**：41 个单品 + 2 个特殊节目（堂吉诃德三篇合并、Roger 中分之歌多版本）。
  标题逐字照搬 B 站原文，未做删改——方括号里的「新春会单品」是作者自己加的标记。
  排序在 `ProgramList.astro` 里按播放量降序统一处理，数据文件不维护顺序。
- **`ProgramList.astro`**：密集节目单布局（序号 / 152px 缩略图 / 标题两行截断 / 作者·日期·播放·时长的元信息行 / 右侧箭头）。
  比原来的卡片宫格更适合 43 项，页面高度从「不可控」变成 12 屏。
- **`MissingList.astro`**：15 个没有可跳转投稿的节目，按「作者有号未发」/「未找到账号」分两组。
  加这一节是为了让节目单完整，不代表对作品的评价。
- **主持人**：阿雨不忘初心（631645354）、筱洛Serov（21154649）。
- **`/announcement` 公告栏**：新页面 + 侧栏第 5 项。3 条公告（PV 公开、报名开放、抄袭处理声明）。

### 三个特殊情况怎么处理的

1. **江山行于舟《机动特遣队·乡里愚人×为什么》**：从 FoundCeremony 的 B 站动态取到公告原文（`x/polymer/web-dynamic` 返回 -352，改用浏览器读页面拿到）。公告全文进 `/announcement#2026-plagiarism`，节目单的未归档清单里给出跳转链接，note 只写「经核实为抄袭作品，作者已改署为 MAKU_050」。
2. **堂吉诃德-骑士**：《基金会也要过春节》三篇合并成一个节目。没有写任何说明，只用一个「多篇合并」标记 + 三个分篇链接（组织篇 / 异常篇 / 至高神性篇）。播放量取三支之和（内容不重叠）。
3. **Roger_F《中分之歌》**：MV 与不秀海鸥的合唱录制版合为一条，标「AI 梗曲」，note 说明「《中分之歌》是 AI 生成的跑调梗曲，大合唱版未见官方投稿，一并收录投稿者不秀海鸥录制的版本」。播放量只取官方 MV 一支（另一支是同曲）。

### 封面

`public/covers/` 拆成两档：正片与官方物料的 4 张保持 880px，其余 46 张压到 320px（q78）。
总计 842 KB，原来是 12 张 880px 占 656 KB。`dist/covers` 948 KB。

### 踩坑

- **`x/space/acc/info` 与 `x/polymer/web-dynamic/v1/detail` 都会被风控**（-799 / -352），带 buvid3 也一样。
  主持人名称与公告正文最后是用 Playwright 打开页面直接读文本拿到的。
- 拉 51 个视频元数据时，`BV1ssYu6XEWC`（2027 PV）返回 `code=62003 稿件已审核通过，等待发布中`——反证了它确实已定时投稿。

### 验证

回归脚本扩到 **94 项**，新增：节目单 43 行、每行都有 B 站链接、43 张缩略图全部可达（逐张发请求验状态码）、序号连续、
两个特殊节目的标记与分篇链接、未归档清单 15 条、主持人两位、公告 3 条带锚点、声明出处、侧栏 5 项与高亮。
对 `dist/` 生产构建跑 **94/94 通过**。

### 产物

`dist/` 1.7 MB（covers 948 KB）；2026 页 HTML 152 KB（43 行 + 首屏），其余页 40–60 KB。
